# MKULIMA Seedlings Ltd - Robots.txt
# Allow all search engines to crawl the website

User-agent: *
Allow: /

# Sitemap location
Sitemap: https://www.mkulimaseedlings.com/sitemap.xml

# Admin and API are kept out of search via a noindex header (X-Robots-Tag),
# not by Disallow — blocking crawl here would leave the URLs "indexed without
# content". Letting Google crawl them lets it see noindex and drop them.

# Crawl delay (optional - helps prevent server overload)
Crawl-delay: 1